# Fair Harvest Web

Next.js frontend for the Fair Harvest platform.

## Run

```bash
npm install
copy .env.example .env.local
npm run dev
```

Open `http://localhost:3000`.

The app expects the Node API at `NEXT_PUBLIC_API_BASE_URL`, defaulting to `http://localhost:4000`.
